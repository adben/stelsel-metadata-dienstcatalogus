---
openapi: 3.0.3
info:
  title: stelsel-metadata-dienstcatalogus API
  version: 1.0.0-SNAPSHOT
paths:
  /authenticatiediensten:
    get:
      tags:
      - Authenticatiedienst Resource
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                uniqueItems: true
                type: array
                items:
                  $ref: '#/components/schemas/AuthenticatieDienst'
    post:
      tags:
      - Authenticatiedienst Resource
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthenticatieDienst'
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                uniqueItems: true
                type: array
                items:
                  $ref: '#/components/schemas/AuthenticatieDienst'
    delete:
      tags:
      - Authenticatiedienst Resource
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthenticatieDienst'
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                uniqueItems: true
                type: array
                items:
                  $ref: '#/components/schemas/AuthenticatieDienst'
  /authenticatiediensten/simple:
    get:
      tags:
      - Authenticatiedienst Resource
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                uniqueItems: true
                type: array
                items:
                  $ref: '#/components/schemas/AuthenticatieDienst'
  /dienstverlener:
    get:
      tags:
      - Dienstverlener Resource
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                uniqueItems: true
                type: array
                items:
                  $ref: '#/components/schemas/Dienstverlener'
    post:
      tags:
      - Dienstverlener Resource
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Dienstverlener'
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                uniqueItems: true
                type: array
                items:
                  $ref: '#/components/schemas/Dienstverlener'
    delete:
      tags:
      - Dienstverlener Resource
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Dienstverlener'
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                uniqueItems: true
                type: array
                items:
                  $ref: '#/components/schemas/Dienstverlener'
  /dienstverlener/simple:
    get:
      tags:
      - Dienstverlener Resource
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                uniqueItems: true
                type: array
                items:
                  $ref: '#/components/schemas/Dienstverlener'
components:
  schemas:
    AuthenticatieDienst:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        clients:
          type: array
    Dienstverlener:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        providers:
          type: array
